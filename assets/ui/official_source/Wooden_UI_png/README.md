# Wooden UI Source Pack

Imported from local source:

`C:\Users\dovig\Downloads\wooden-ui\Wooden_UI_png`

Scope:
- PNG assets are imported as legacy/source UI material.
- The PSD source file is intentionally not imported.
- Runtime backgrounds are intentionally not replaced by this pass.
- This pack is no longer the primary UI pack for overhaul work. Use `assets/ui/third_party/gowl_stonepixel/` for StonePixel-backed runtime surfaces.

Current mapping status:
- Primary button styleboxes now use StonePixel-backed resources.
- Main panel stylebox now uses StonePixel-backed resources.
- `BettingCircle.tscn` now uses StonePixel-backed registry styleboxes; book-era PNGs here are legacy source assets only.
