# CANON â€” MECHANICS UNIFIED

Status: Single source of truth

If another doc conflicts, this doc wins.

Last merged from: docs/game_flow.md, docs/game_flow_v2.md, docs/FLOW_OFFICIAL_EA.md, docs/bet_progression.md, docs/meta_progression.md, docs/scar_system.md

## Canon Contract

This document is authoritative for its category.
No other file may redefine these concepts.
All changes to systems described here must update this document in the same PR.


## Index

- [Flow canonico (ordine fisso)](#flow-canonico-ordine-fisso)
- [Checkpoints autosave (canonici)](#checkpoints-autosave-canonici)
- [SOURCE: bet progression](#source-docsbetprogressionmd)
- [SOURCE: meta progression](#source-docsmetaprogressionmd)
- [SOURCE: scar system](#source-docsscarsystemmd)

## SOURCE: docs/game_flow.md

# Game Flow

**Status:** ARCHIVE-CANDIDATE  
**Scope:** Legacy game flow write-up retained for traceability while superseded by canonical contracts.  
**Source of truth:** docs/flow_wiring_contract.md, docs/run_architecture_ledger.md  
**Last updated:** 2026-02-11  
**Notes:** Superseded in practice by docs/flow_wiring_contract.md and docs/game_flow_v2.md.

Overlaps with: docs/FLOW_OFFICIAL_EA.md, docs/game_flow_v2.md.
- Candidate for archive after consolidation patch.

## Overlap
- Overlaps with: docs/FLOW_OFFICIAL_EA.md, docs/game_flow_v2.md.
- Candidate for archive after consolidation patch.

GALLICUS Â· GAME FLOW DIEGETICO

Versione: 1.0 â€” Prima Incisione

0. PRINCIPIO GUIDA

Gallicus non introduce il suo mondo.
Il mondo si manifesta mentre il giocatore agisce.

Il giocatore:

non riceve contesto

non riceve spiegazioni

non riceve obiettivi tradizionali

Riceve registrazioni.

1. INIZIO ASSOLUTO â€” PRIMA RUN

Stato del giocatore

Nessuna conoscenza del mondo

Nessuna conoscenza del Registro

Nessuna aspettativa narrativa

Il gioco non presenta:

lore

personaggi

storia

conflitto esplicito

Solo una situazione operativa.

2. APERTURA DELLA RUN

Evento

La run inizia sempre con una scommessa iniziale.

Non viene spiegato:

perchÃ© si scommette

cosa rappresenta

cosa accade se si rifiuta

Il giocatore capisce solo una cosa:

per andare avanti, deve accettare qualcosa.

3. PRIMA MANIFESTAZIONE DEL REGISTRO

Trigger

Alla prima scelta irreversibile
(o alla prima scommessa che introduce perdita reale).

Forma

Testo breve

Secco

Sovrapposto

Non interattivo

Contenuto (struttura, non testo fisso)

Registrato:
[descrizione neutra dellâ€™atto]

Esempi di struttura:

â€œaccettata una perditaâ€

â€œrinunciata unâ€™alternativaâ€

â€œfirmata una condizioneâ€

Non viene spiegato chi parla.
Non viene spiegato perchÃ©.

4. PERCEZIONE DEL GIOCATORE (INTENZIONALE)

Il giocatore non deve pensare:

â€œAh, questo Ã¨ un narratore.â€

Deve pensare:

â€œQualcosa ha preso nota.â€

Il Registro non interrompe il flow.
Lo taglia per un istante.

5. CONTINUAZIONE DELLA RUN

Stato del Registro

Invisibile

Silenzioso

Presente

Il Registro:

non appare a ogni scelta

non diventa prevedibile

non reagisce a successi o fallimenti

Solo a pattern emergenti.

6. CICLO BASE DELLA PRIMA RUN

Ripetizione implicita:

Scommessa

Risoluzione arena

Conseguenza

PossibilitÃ  di chiusura

Rifiuto o accettazione

(Eventuale) Annotazione del Registro

Il giocatore non sa che:

sta accumulando Scar

sta costruendo un profilo

sta avvicinandosi a una definizione

Ma lo sente.

7. FINE DELLA PRIMA RUN

Caso A â€” Morte precoce

Nessuna lettura finale

Nessuna sentenza

Nessuna spiegazione

Al massimo:

â€œRegistrato: interruzione non conclusiva.â€

Il player percepisce:

â€œNon ho visto tutto.â€

Caso B â€” Arrivo alla Fine del Circuito (raro)

Il Registro parla piÃ¹ a lungo, ma resta impersonale.

Non racconta una storia.
Fa una constatazione aggregata.

Struttura:

apertura neutra

2â€“3 pattern osservati

stato finale

Esempio strutturale:

â€œIl soggetto ha accettato ripetutamente condizioni irreversibili.

Ha rifiutato la chiusura quando disponibile.

Stato finale: definito.â€

Nessuna menzione a Felix Gallicus.
Nessuna anomalia.
Nessuna crepa.

8. USCITA DALLA RUN

Il giocatore torna al menu senza spiegazioni aggiuntive.

Non viene detto:

cosa ha sbloccato

cosa ha imparato

cosa fare ora

Ma resta una domanda implicita:

â€œSe continuoâ€¦ cambierÃ ?â€

9. OBIETTIVO PSICOLOGICO DEL FLOW

Alla fine delle prime run, il giocatore deve:

essere curioso

sentirsi osservato

non sentirsi guidato

non sentirsi giudicato

Il Registro Ã¨ percepito come:

una presenza che aspetta.

10. CONDIZIONE DI FALLIMENTO DEL FLOW

Il flow fallisce se:

il Registro viene spiegato

il player lo ignora

appare come UI di feedback

sembra â€œcoolâ€ o teatrale

Il Registro deve sembrare inevitabile, non interessante.

INCISIONE DI CHIUSURA

Allâ€™inizio, Gallicus non racconta una storia.
Registra un comportamento.

La storia verrÃ  letta solo quando sarÃ  troppo tardi per cambiarla.

## SOURCE: docs/game_flow_v2.md

# Game Flow V2

**Status:** SUPPORTING  
**Scope:** Updated flow narrative aligned to current Level 3 run loop.  
**Source of truth:** docs/flow_wiring_contract.md, docs/run_architecture_ledger.md  
**Last updated:** 2026-02-11  
**Notes:** Overlaps with: docs/FLOW_OFFICIAL_EA.md, docs/game_flow.md.

## Overlap
- Overlaps with: docs/FLOW_OFFICIAL_EA.md, docs/game_flow.md.

GALLICUS Â· GAME FLOW â€” FASE 2

Versione: 2.0 â€” Emergenza dellâ€™Anomalia

0. PRINCIPIO GUIDA DELLA FASE 2

La Fase 2 non introduce nuovi sistemi.
Introduce dubbio.

Il giocatore:

conosce il Registro

ne accetta lâ€™autoritÃ 

si fida della sua coerenza

La Fase 2 serve a incrinare questa fiducia.

1. CONDIZIONE DI ACCESSO ALLA FASE 2

La Fase 2 non Ã¨ legata a:

numero di run

tempo di gioco

skill del player

Si attiva quando una run soddisfa una configurazione rara:

alto numero di Scar irreversibili

pattern di rischio coerente ma estremo

rifiuto sistematico della chiusura

accettazione consapevole di perdite non necessarie

ðŸ‘‰ Il sistema determina che:

il soggetto non Ã¨ solo definito,
ma eccede le categorie disponibili.

2. PRIMA CREPA â€” IL REGISTRO ESITA
Evento

Durante una registrazione, qualcosa cambia.

Non il contenuto.
La forma.

Differenze minime ma percepibili

una pausa piÃ¹ lunga

una frase meno standard

una struttura leggermente deviata

Esempio strutturale (non testo definitivo):

â€œRegistrato:
accettata una perdita che non risulta necessaria
secondo i parametri correnti.â€

âš ï¸ Ãˆ la prima volta che il Registro:

fa riferimento ai propri parametri

suggerisce una discrepanza

3. PERCEZIONE DEL GIOCATORE (OBIETTIVO)

Il giocatore non deve pensare:

â€œIl gioco Ã¨ buggato.â€

Deve pensare:

â€œQuesto non doveva succedere.â€

Il Registro non spiega.
Non si corregge.
Va avanti.

4. DEFINIZIONE DELLâ€™ANOMALIA
Cosâ€™Ã¨ unâ€™Anomalia

Unâ€™Anomalia Ã¨ un soggetto che:

soddisfa i criteri di definizione

senza rientrare in nessuna classe narrativa prevista

pur rispettando tutte le regole

Non Ã¨:

un errore

un exploit

una scelta segreta

Ãˆ:

un esito che il Registro puÃ² registrare,
ma non interpretare.

5. INTRODUZIONE DI FELIX GALLICUS
Prima menzione (indiretta)

Il nome non viene spiegato.
Appare come referenza storica.

Esempio strutturale:

â€œNota di precedenza rilevata.

Caso analogo: Felix Gallicus.â€

Nessuna descrizione.
Nessun contesto.

Il nome Ã¨:

freddo

amministrativo

trattato come un ID storico

6. COSA IL REGISTRO NON DICE (INTENZIONALE)

Il Registro non chiarisce:

chi Ã¨ Felix

cosa ha fatto

perchÃ© Ã¨ rilevante

Il player deve sentire:

â€œNon sono il primo.
Ma forse nessuno Ã¨ andato oltre.â€

7. CAMBIO DI TONO (GRADUALE)

Dopo la prima crepa:

il Registro resta impersonale

ma diventa meno assoluto

compaiono riferimenti a:

parametri

precedenti

soglie non documentate

Non emozione.
Burocrazia che scricchiola.

8. FINE DI UNA RUN ANOMALA
Lettura finale â€” Variante

Il finale non Ã¨ una sentenza pienamente chiusa.

Struttura:

constatazione

riferimento a incongruenza

classificazione incompleta

Esempio strutturale:

â€œIl soggetto presenta un profilo coerente.

La classificazione non Ã¨ conclusiva.

Stato: registrato in attesa.â€

âš ï¸ Ãˆ la prima volta che:

la run non viene chiusa narrativamente

il Registro non dichiara definizione

9. USCITA DALLA RUN (EFFETTO PSICOLOGICO)

Il player torna al menu con una certezza nuova:

â€œIl Registro non Ã¨ infallibile.
Ma io non so perchÃ©.â€

Ed Ã¨ qui che nasce la spinta.

Non per vincere.
Per capire cosa Ã¨ successo a Felix Gallicus
e perchÃ© il sistema non sa piÃ¹ come leggerlo.

10. CONDIZIONE DI FALLIMENTO DELLA FASE 2

La Fase 2 fallisce se:

Felix viene spiegato subito

il Registro diventa emotivo

lâ€™anomalia sembra una â€œroute segretaâ€

il player capisce troppo

Il dubbio deve essere:

strutturale, non narrativo.

INCISIONE FINALE

**Il Registro non perde autoritÃ  quando sbaglia.

La perde quando incontra qualcosa
che non Ã¨ stato progettato per capire.**

## SOURCE: docs/FLOW_OFFICIAL_EA.md

# GAL LICUS â€” Flow Ufficiale EA (Repo-Aligned Contract)

**Status:** SUPPORTING  
**Scope:** Repository-aligned Level 3 flow narrative and signal sequence reference.  
**Source of truth:** docs/flow_wiring_contract.md, docs/run_architecture_ledger.md  
**Last updated:** 2026-02-11  
**Notes:** Overlaps with: docs/game_flow_v2.md, docs/game_flow.md, docs/flow_wiring_contract.md.
- Candidate for archive after consolidation patch if fully subsumed by canonical flow contract.

## Overlap
- Overlaps with: docs/game_flow_v2.md, docs/game_flow.md, docs/flow_wiring_contract.md.
- Candidate for archive after consolidation patch if fully subsumed by canonical flow contract.

## Scopo
Documento canonico del flow â€œLevel 3â€ implementato nel repo corrente.
Serve per debugging, prevenzione regressioni e memoria dei passaggi.

## Principi invarianti
1) RunManager Ã¨ autoritÃ  di stato e progressione.
2) UI Ã¨ reattiva (emette solo request_*).
3) Comunicazione cross-layer solo via GameEvents.
4) Il flow runtime Ã¨ esplicito via `RunPhase` enum in `run_manager.gd`.

## Level 3 Outcome Vocabulary (Canon)

### Scopo
Definire il vocabolario **autoritativo** del payload outcome Level 3 in termini rituali,
senza semantiche esposte di combattimento (risk-only ritual contract).

### Schema canonico payload outcome (ritual-only)

Payload minimo raccomandato per `arena_completed` / superfici UI derivate:

- `risk_profile: String`
  - Profilo di rischio rituale applicato alla risoluzione.
- `pressure_mod: float`
  - Modificatore di pressione rituale (no stat semantics).
- `failure_chance: float`
  - ProbabilitÃ  di esito avverso rituale determinata da bet/RNG.
- `condemnation_flag: bool`
  - Indicatore esplicito che la risoluzione ha prodotto condanna/classificazione avversa.
- `outcome_tier: String`
  - Classe rituale dell'esito (es. favorevole / avverso / terminale secondo canon runtime).
- `outcome_reason: String`
  - Motivazione rituale/registrale per log/UI (mai linguaggio combat).

Note canoniche:
- AutoritÃ  outcome Level 3: **solo patto + bet + RNG**.
- Combat runtime Ã¨ disabilitato: nessuna autoritÃ  terminale da sistemi fisici legacy.
- Runtime Level 3 does not expose or consume legacy penalty signals/fields in active flow/UI wiring.
- Level 3 run save payload includes `level3_schema = 2` as one-time migration boundary for sealed loss-consequence contract.
- `glory` e `corruption` restano invarianti run-level in `RunState` e non ridefiniscono semantica combat.

### Legacy combat semantics â†’ stato C6B

Le alias legacy di perdita/esito sono state rimosse dal contratto runtime Level 3 attivo.
Le superfici runtime e UI devono usare esclusivamente chiavi canoniche rituali.

### Regola di migrazione (obbligatoria)

- Le legacy keys sono **solo dettaglio implementativo temporaneo**.
- Le legacy keys non devono essere esposte in UI, HUD, copy o telemetria di prodotto.
- UI/runtime contract deve bindare esclusivamente alle chiavi rituali canoniche.
- RunManager branching di flow (es. fail di patto e audience delta) deve leggere solo chiavi canoniche rituali (`condemnation_flag`, `corruption_gain`, `end_reason`).
- In C6B runtime, legacy outcome/loss aliases are removed from active payload emission; migration table remains historical reference only.
- Ogni nuova surface utente deve mantenere lessico rituale e amministrativo nella semantica outcome Level 3.

## Nomi reali (GameEvents)
- Avvio/continua: request_new_run, request_continue_run
- Betting: request_place_bet, bet_ui_opened, bet_placed
- Rituali: pact_sealed_opened/closed, resolve_ritual_opened/closed
- Arena: arena_started, arena_completed (emette solo arena_index)
- Post-arena: request_mid_choice_select, push_luck_opened,
  request_pyl_cashout, request_pyl_double
- Run end: run_finale_selected, run_failed
- Menu return: request_show_main_menu
- Save: SaveManager.clear_run() chiamato in end-run

---

## Flow canonico (ordine fisso)

### 0) MENU
RunPhase: MAIN_MENU  
UI: menu principale  
UI â†’ RunManager: request_new_run, request_continue_run

### 1) RUN START (Level 3)
RunPhase: RUN_INIT  
RunManager: _start_level3_run() â†’ run_started â†’ start_arena()

### 2) ARENA SETUP
RunPhase: BET_PRESENT (entry dopo setup arena)  
RunManager: start_arena():
- arena_index++
- tema/special arena
- profilo nemici
- apre betting Level 3

### 3) BET UI OPEN
RunPhase: BET_PRESENT  
RunManager â†’ UI: bet_ui_opened  
UI â†’ RunManager: request_place_bet(bet_id)
- Offerte canoniche BET_PRESENT (L3 attivo): `CASH_OUT`, `DOUBLE_OR_DIE` (shape a 2 opzioni).

### 4) BET SELECTED + CHECKPOINT
RunPhase: BET_COMMITTED  
RunManager: select_bet():
- registra bets_history/condanne
- emette bet_placed
- chiude UI bet
- AUTOSAVE: RUN_FLOW_BET_SIGNED
- avvia rituali/risoluzione

### 5) RITUAL 1 â€” PACT SEALED
RunPhase: BET_COMMITTED (rituale in flow interno)  
RunManager â†’ UI: pact_sealed_opened  
Chiusura: pact_sealed_closed emesso dal RunManager (timer/flow interno)

### 6) REAZIONE PUBBLICO + GESTURE
RunPhase: INTERMEDIATE_CHOICE  
Dopo `pact_sealed_closed` il RunManager apre direttamente la fase gesto (senza `POST_BET_MESSAGES` nel flow attivo).  
RunManager â†’ UI: payload INTERMEDIATE_CHOICE con messaggio pubblico + opzioni gesto  
UI â†’ RunManager: request_mid_choice_select(0|1)  
AUTOSAVE: RUN_FLOW_PUSH_LUCK

### 7) RITUAL 2 â€” RESOLVE RITUAL + ARENA RESOLVE
RunPhase: RESOLUTION  
Dopo la scelta gesto, il RunManager avvia `resolve_ritual_opened/closed` e poi risolve arena:
- arena_started
- arena_completed(arena_index)
- applica outcome, aggiorna audience
AUTOSAVE: RUN_FLOW_INTERMEDIATE_CHOICE (subito dopo la risoluzione)

### 8) PUSH YOUR LUCK
RunPhase: PUSH_YOUR_LUCK  
RunManager â†’ UI: push_luck_opened(payload)  
UI â†’ RunManager:
- request_pyl_cashout
- request_pyl_double

Regole canoniche L3 nel segmento PUSH_YOUR_LUCK:
- Audience Pressure: valore run-scoped che cresce sui rilanci e su esiti avversi, cala su cashout/chiusura, e modula il cashout modifier nel payload UI.
- Deferred Signature Echo: scelta gesto `provoca` su patto ad alto rischio arma un bonus differito consumato al primo double successivo.
- Escalation Threshold Events: superamento soglie escalation (3/5/7) attiva eventi one-shot run-scoped (condanne + incremento corruzione) sotto sola autorita RunManager.

Checkpoint:
- Double â†’ AUTOSAVE RUN_FLOW_BET_OFFER e ritorno a ARENA_SETUP
- Cashout â†’ end-run (nessun autosave; clear_run a fine)

### 10) RUN END â€” VERDETTO
RunPhase: GAME_OVER  
RunManager:
- SaveManager.clear_run()
- run_finale_selected(payload)
- se loss: run_failed
- classificazione terminale `THE_FALL` se corruzione hidden per-run `>= 5`
- `THE_FALL` non viene classificato quando `run_end_reason == "INFRA_FAILURE"`
UI:
- VERDETTO
- bottoni: Nuova Run / Torna al Menu (request_show_main_menu)

Registro (provvisorio/finale):
- END_RUN espone sempre `meta.register_message`.
- `register_final` Ã¨ `false` finchÃ© `completed_bets < 3` (mai finale al primo giro).
- Quando `completed_bets >= 3`, la chiusura fascicolo Ã¨ deterministica in prioritÃ : `corruption >= 70` â†’ `ending_corruption`; altrimenti `glory >= 100` â†’ `ending_glory`; altrimenti `scars_count >= 3` â†’ `ending_scars`; altrimenti `ending_pattern`.
- In Registro Finale: `meta.next_bet_enabled = false`, il run Ã¨ terminale, `register_ending_key` Ã¨ obbligatorio e l'esito meta Ã¨ emesso una sola volta per run.
- Wiring achievements canonico (sistema unlock esistente via Condanne/SaveManager):
  - `ending_corruption` â†’ `CONDANNA_REGISTRO_COMPROMISSIONE`
  - `ending_glory` â†’ `CONDANNA_REGISTRO_ASCESA`
  - `ending_scars` â†’ `CONDANNA_REGISTRO_CONSUMO`
  - `ending_pattern` â†’ `CONDANNA_REGISTRO_PATTERN`
- On finale resta emesso anche `GameEvents.meta_progress_unlocked(ending_key)` una sola volta per run; `archive_entry_unlocked` resta side-effect meta separato.

---

## Checkpoints autosave (canonici)
1) Dopo firma patto â†’ RUN_FLOW_BET_SIGNED
2) Dopo scelta gesto â†’ RUN_FLOW_PUSH_LUCK
3) Dopo risoluzione arena â†’ RUN_FLOW_INTERMEDIATE_CHOICE
4) Dopo scelta Double â†’ RUN_FLOW_BET_OFFER
5) End-run â†’ clear_run()

---

## Bug hotspots
- Rituali: open senza close â†’ softlock (timer/flow interno)
- Betting: mismatch segnali request_place_bet
- arena_completed senza payload outcome â†’ UI deve derivare da RunState
- Legacy BetManager attivo insieme a Level 3 â†’ flow misto (da evitare)

## Regola dâ€™oro
Se una UI non appare: non Ã¨ layout.  
Ãˆ un open non emesso, un close non emesso o un listener non connesso.

## SOURCE: docs/bet_progression.md

# Bet Progression

**Status:** SUPPORTING  
**Scope:** Design doctrine for bet progression and risk escalation tone.  
**Source of truth:** docs/00_RISK_DRIVEN_DESIGN_BIBLE, docs/game_flow_v2.md  
**Last updated:** 2026-02-11  
**Notes:** Overlaps with: docs/game_flow.md, docs/game_flow_v2.md.

## Overlap
- Overlaps with: docs/game_flow.md, docs/game_flow_v2.md.

GALLICUS Â· PROGRESSIONE DELLE SCOMMESSE

Versione: 1.0 â€” Fondazionale

1. PERCHÃ‰ ESISTONO LE SCOMMESSE

Le scommesse non servono a vincere di piÃ¹.
Servono a rimandare la definizione del soggetto.

Ogni scommessa Ã¨ un atto di rifiuto della chiusura.

Il player non scommette perchÃ© conviene.
Scommette perchÃ© fermarsi ora significherebbe accettare unâ€™identitÃ  incompleta.

2. COSA OFFRONO DAVVERO

Una scommessa offre:

continuitÃ 

accesso a ulteriori decisioni

illusione di controllo

Non offre:

sicurezza

stabilitÃ 

vera crescita

3. STRUTTURA DELLA PROGRESSIONE

La progressione delle scommesse Ã¨ qualitativa, non numerica.

Ogni step:

riduce le opzioni sicure

introduce condanne piÃ¹ nette

aumenta la probabilitÃ  di Scar

La curva NON Ã¨:

piÃ¹ reward

piÃ¹ difficoltÃ 

Ãˆ:

meno via dâ€™uscita

4. PUSH YOUR LUCK â€” SIGNIFICATO REALE

Il push your luck non chiede:

â€œVuoi rischiare?â€

Chiede:

â€œAccetti di non chiudere ancora?â€

Ogni rifiuto della chiusura:

aumenta il peso narrativo della run

avvicina il soggetto alla Fine del Circuito

alimenta il Registro

5. RELAZIONE CON LE SCAR

Le scommesse non creano sempre Scar

Ma preparano il terreno

Superate certe soglie â†’ la Scar Ã¨ inevitabile

Il player non sa quando.
Il sistema sÃ¬.

6. CONDIZIONE DI FINE

La run termina quando:

il sistema considera il soggetto definito

non esistono piÃ¹ scommesse significative

la prosecuzione sarebbe solo reiterazione

A quel punto:

Il Registro prende la parola.

7. FALLIMENTO DEL SISTEMA

Il sistema scommesse fallisce se:

il player cerca â€œla scelta giustaâ€

il rischio viene letto come ottimizzazione

la chiusura non fa paura

CHIUSURA â€” INCISIONE NELLA PIETRA

Questi due sistemi esistono per una sola ragione:

Permettere al gioco di raccontare una storia
senza mai raccontarla durante la run.

Il player non legge la storia.
La subisce alla fine.

## SOURCE: docs/meta_progression.md

# GALLICUS Â· META PROGRESSIONE

**Status:** SUPPORTING  
**Scope:** Meta-progression design boundaries and unlock philosophy.  
**Source of truth:** docs/00_RISK_DRIVEN_DESIGN_BIBLE, docs/CODEX_GOLDEN_CHECKLIST.md  
**Last updated:** 2026-02-11  
**Notes:** Overlaps with: docs/bet_progression.md, docs/scar_system.md.

## Overlap
- Overlaps with: docs/bet_progression.md, docs/scar_system.md.

Versione: 1.0 â€” Persistenza della Conoscenza

## 0. PRINCIPIO FONDANTE

Gallicus non ricompensa la ripetizione.  
Ricompensa la comprensione del rischio.

Tra una run e lâ€™altra non sopravvive il potere.  
Sopravvive solo ciÃ² che non puÃ² piÃ¹ essere disimparato.

## 1. COSA Ãˆ LA META-PROGRESSIONE

La meta-progression in Gallicus Ã¨:

accumulo di consapevolezza sistemica.

Non Ã¨:

- crescita numerica
- sblocco di vantaggi
- facilitazione delle run future

## 2. COSA PERSISTE TRA LE RUN

### 2.1 Persistono solo ENTITÃ€ DI CONOSCENZA

Esempi ammessi:

- nuove classi di scommesse (piÃ¹ rischiose)
- nuove condanne narrative
- nuove soglie di irreversibilitÃ 
- nuove modalitÃ  di chiusura

ðŸ‘‰ Ogni sblocco aumenta il rischio potenziale, mai la sicurezza.

### 2.2 Registry references (boundary note)

La descrizione diegetica di come il Registro "ricorda" il player Ã¨ stata spostata in `docs/canon/LORE_UNIFIED.md` per separazione categorie.

In Mechanics resta solo il vincolo runtime:

- nessuna memoria esplicita lato UI
- nessuna schermata riassuntiva persistente

## 3. COSA NON PERSISTE (DIVIETI ASSOLUTI)

Tra le run non devono mai persistere:

- HP
- danni
- bonus
- risorse
- upgrade
- valuta
- mitigazioni del rischio

Runtime enforcement note (Level 3): active run flow does not use XP/level-up/upgrade-token progression signals or UI loops.
Runtime enforcement note (Level 3): stat upgrades (`hp_bonus`, `light_bonus`, `heavy_bonus`) have no gameplay effect; outcome resolution remains bet-curve/random flow authority.
Runtime enforcement note (Level 3): combat runtime is disabled; success/failure is resolved by bet/RNG flow only.
Runtime enforcement note (Level 3): Player is visual-only (`Node2D`) and movement runtime is inert (no physics-driven movement authority).

Se qualcosa rende la prossima run:

piÃ¹ facile

ðŸ‘‰ Ã¨ illegale.

## 4. RUOLO DI FELIX NELLA META-PROGRESSIONE (boundary note)

Le implicazioni narrative su Felix Gallicus sono state spostate in `docs/canon/LORE_UNIFIED.md` per evitare leak cross-category.

In Mechanics resta solo il vincolo sistemico:

- la meta progression non sblocca vantaggi numerici
- le nuove opzioni aumentano rischio e irreversibilitÃ 

## 5. STRUTTURA DEGLI SBLOCCHI

Ogni sblocco deve rispettare questa formula:

PiÃ¹ avanti â†’ meno ritorno â†’ piÃ¹ irreversibilitÃ 

Esempio strutturale:

- nuova scommessa con payoff alto
- condizione piÃ¹ stretta
- condanna piÃ¹ devastante

Mai:

- â€œora sei piÃ¹ forteâ€
- â€œora puoi correggereâ€

Sempre:

â€œora puoi rischiare in modo piÃ¹ definitivo.â€

## 6. RELAZIONE CON IL FINALE

La meta-progression:

- non cambia il finale passato
- non riscrive la storia
- non aggiunge epiloghi retroattivi

Cambia solo:

quanto in lÃ  puoi spingerti la prossima volta.

## 7. OBIETTIVO PSICOLOGICO

Il player deve sentire:

â€œSo di piÃ¹.  
Ma per questoâ€¦ posso farmi molto piÃ¹ male.â€

Non:

â€œOra posso farcela.â€

## 8. CONDIZIONE DI FALLIMENTO DEL SISTEMA

La meta-progression fallisce se:

- il player grind-a
- cerca efficienza
- accumula sicurezza
- sente â€œprogressioneâ€

Se il player dice:

â€œFinalmente sono pronto.â€

ðŸ‘‰ design fallito.

Se dice:

â€œOra so quanto puÃ² andare male.â€

ðŸ‘‰ design riuscito.

## INCISIONE FINALE

In Gallicus, la vera progressione  
Ã¨ rendere possibile una sconfitta  
che prima non potevi nemmeno immaginare.

## SOURCE: docs/scar_system.md

# Scar System

**Status:** SUPPORTING  
**Scope:** Design and behavior notes for scars as persistent run consequences.  
**Source of truth:** docs/00_RISK_DRIVEN_DESIGN_BIBLE, docs/run_architecture_ledger.md  
**Last updated:** 2026-02-11  
**Notes:** Overlaps with: docs/bet_progression.md, docs/meta_progression.md.

## Overlap
- Overlaps with: docs/bet_progression.md, docs/meta_progression.md.

GALLICUS Â· SCAR SYSTEM

Versione: 1.0 â€” Fondazionale

1. DEFINIZIONE

Una Scar Ã¨ una registrazione irreversibile di una scelta ad alto rischio.
Non rappresenta una penalitÃ , ma una perdita di reversibilitÃ .

Una Scar esiste perchÃ© il sistema ha preso atto
che il soggetto ha accettato di non poter piÃ¹ tornare indietro.

2. OBIETTIVO DEL SISTEMA

Il sistema delle Scar ha un solo obiettivo:

Rendere il finale narrativo inevitabile.

Le Scar NON servono a:

bilanciare la difficoltÃ 

punire il giocatore

rallentare la run

Servono a:

definire lâ€™identitÃ  della run

costruire pattern leggibili dal Registro

impedire finali neutri

3. PRINCIPI NON NEGOZIABILI

3.1 IrreversibilitÃ  assoluta

Una Scar non puÃ² mai essere rimossa

Non esistono cure, cleanse, reset parziali

La run puÃ² solo accumularle

3.2 Nessuna Scar Ã¨ neutra

Ogni Scar:

chiude almeno una possibilitÃ  futura

forza il player verso una direzione identitaria

Se una Scar â€œaggiunge solo qualcosaâ€ â†’ Ã¨ illegale.

3.3 Le Scar non si spiegano

Il gioco non descrive mai lâ€™effetto finale narrativo

Il player vede solo lâ€™effetto sistemico immediato

Il significato emerge solo nel finale

4. STRUTTURA DI UNA SCAR

Ogni Scar Ã¨ composta da tre strati:

A. Effetto Sistemico

Modifica regole della run:

lock di opzioni

alterazione delle scommesse

deviazione delle probabilitÃ 

B. Tag Identitario (invisibile)

Ogni Scar assegna tag interni, ad esempio:

excess

avoidance

submission

complicity

defiance

I tag non sono mostrati al player.
Servono solo al Registro.

C. Valenza Narrativa

La Scar Ã¨ una frase implicita, es.:

â€œhai accettato una perdita non necessariaâ€

â€œhai rinunciato al controllo in cambio di continuitÃ â€

Questa frase non viene mai mostrata,
ma sarÃ  letta nel finale.

5. QUANDO NASCE UNA SCAR

Una Scar nasce solo se:

una scommessa introduce irreversibilitÃ 

una condizione viene accettata consapevolmente

il player rifiuta piÃ¹ volte una chiusura disponibile

una run supera soglie di rischio cumulative

Le Scar non sono frequenti.
Ogni run deve poterne avere poche, ma decisive.

6. RELAZIONE CON IL REGISTRO

Ogni Scar viene registrata

Il Registro non commenta

Il Registro annota lâ€™atto, non lâ€™esito

Esempio di logica diegetica:

â€œRegistrato: accettata una perdita che non era necessaria.â€

7. CONDIZIONE DI FALLIMENTO DEL SISTEMA

Il sistema Scar fallisce se:

una run puÃ² arrivare al finale senza Scar rilevanti

le Scar sono percepite come â€œmalusâ€

il player tenta di â€œottimizzarleâ€.


## Registry State & Destiny Conditions (Mechanical Addendum)

- Global persistent hidden flag: `registry_has_precedent` (default `false`).
- `registry_has_precedent` becomes 	rue` after the first completed run with a classified terminal outcome.
- The player is never informed explicitly.

### Liberty condition (first era only)

`LIBERTY` is eligible only when:

- `registry_has_precedent == false`
- all hidden generation constraints for liberation are satisfied.

If `registry_has_precedent == true`, `LIBERTY` is never eligible.

### Registry systemic effect after precedent

When `registry_has_precedent == true`, generation shifts toward reiteration pressure:

- liberation paths are not generated,
- anomaly recurrence becomes more frequent,
- classification outcomes prefer continuity over closure.

No explicit gameplay modifier UI is exposed.

### Corruption scope

`Registry Corruption` is not a numeric gameplay variable and not a player-facing bar.
It is a narrative-classification layer expressed through language, recurrence and contextual tone.

Run runtime keeps an internal per-run integer `corruption` (default `0`) as RunManager authoritative runtime field (hard cap `100`).
It is incremented only by RunManager at authoritative ingestion points:

- push-your-luck `double` requests (canonical `+1`),
- high-risk pact selection (canonical `+1`),
- passive Scar pool triggers (double pool / high-risk pact pool),
- Level 3 loss consequences carrying canonical `corruption_gain` (clamped to `100`).

Level 3 passive Scar contract (RunManager-only):

- Scar triggers are random and save/load deterministic through a sealed per-run RNG state (`scar_rng_state` + `scar_roll_index`).
- Double Scar pool: chance table by doubles count (`0.10 / 0.20 / 0.35 / 0.50` cap) with per-pool diminishing returns (`1 / (1 + scar_double_count)`).
- High-risk pact Scar pool: base chance scales with run progress (`arena_index`) and applies the same per-pool diminishing rule (`1 / (1 + scar_pact_count)`).
- High-risk pact corruption/Scar ingestion is idempotent per pact confirmation context (same `arena_index` + `bet_id` cannot be ingested twice), and guard state is written only on effective high-risk ingestion.
- Scar triggers are irreversible and not removable.
- `volatility` is run-scoped and increases only from passive Scar triggers/pressure.

Level 3 loss consequence contract is hard-sealed to canonical `corruption_gain` + `end_reason`; legacy alias keys are removed from active contract.
No UI exposure is allowed.

### Glory scope

Run runtime keeps an internal per-run integer `glory` (default `0`) in `RunState`.
It is reset at new-run creation and serialized in run save/load payloads with the run state.

`glory` is incremented only by RunManager on successful arena resolution outcomes:

- `glory += GLORY_PER_SUCCESS * glory_multiplier`
- `GLORY_PER_SUCCESS = 1`
- `glory_multiplier` starts at `1` and increases only on push-your-luck `double/continue`
  according to the authoritative RunManager step table (`1, 2, 4, 7, 11`).

Glory is displayed in HUD as a numeric value only (no explanatory gameplay logic in UI).


### Invariants

- Runs remain mechanically self-contained.
- Registry state never rewrites completed runs.
- Corruption does not directly alter combat mechanics or player stats.
- Registry state affects generation probabilities and liberty eligibility only.
- Outcome emission still passes through the standard terminal flow authority.





